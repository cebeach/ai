name: glob_discovery
fixture: basic_files
prompt: List all .md and .py files in the workspace.
expect:
  stdout_contains:
    - beta.md
  stdout_contains_re:
    - 'src[\s/]*nested[\s/]*sample\.py'
  files_unchanged: true
